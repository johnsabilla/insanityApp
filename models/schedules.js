schedules = new Mongo.Collection('schedules');


if (Meteor.isClient) {

  schedules.allow({
    insert : function () {
      return true;
    },
    update : function () {
      return true;
    },
    remove : function () {
      return true;
    }
  });
}



if (Meteor.isServer) {
  schedules.allow({
    insert : function () {
      return true;
    },
    update : function () {
      return true;
    },
    remove : function () {
      return true;
    }
  });
}
